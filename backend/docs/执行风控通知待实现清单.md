# 执行层、风控层、通知系统待实现清单

本文档记录当前代码中已经预留但尚未完整实现的方法和能力。

## 执行层

### 富途适配器

- `FutuBrokerAdapter.cancel_order`
- `FutuBrokerAdapter.modify_order`
- `FutuBrokerAdapter.query_order`
- 富途订单状态回调处理
- 富途成交回调处理
- 富途 API 异常统一转换
- 富途实盘/模拟盘启动配置装配

### 执行服务

- `ExecutionRepository.insert_operation`
- `ExecutionRepository.insert_trade_record`
- `ExecutionRepository.get_operations` 的数据库版本
- `ExecutionRepository.insert_deal_record` 的数据库版本
- `ExecutionRepository.sync_asset` 的数据库版本
- `ExecutionRepository.get_deals` 的数据库版本
- `ExecutionRepository.get_assets` 的数据库版本
- `ExecutionRepository.get_positions` 的数据库版本
- 订单状态同步
- 成交同步
- 持仓历史同步
- 失败重试机制
- 超时处理
- 异常恢复
- TWAP 算法单
- 条件单
- 止损单
- 止盈单
- 交易日志落库

### API

- `GET /api/v1/dashboard/asset-summary`
- `GET /api/v1/dashboard/position-overview`
- `POST /api/v1/execution/sync-asset`，如后续需要手动触发资产同步
- `GET /api/v1/orders` 的完整筛选参数
- `GET /api/v1/deals` 的完整筛选参数
- `GET /api/v1/positions/history` 的真实数据版本
- `GET /api/v1/logs/trading` 的真实数据版本

## 风控层

- 撤单失败监控
- 改单失败监控
- API 异常监控的完整采集
- 委托超时监控
- 单日亏损超限计算
- 最大回撤超限计算
- 连续亏损次数超限计算
- 账户资产异常波动检测
- 持仓异常变化检测
- 风险趋势真实计算
- 风险事件状态流转接口
- 熔断后的执行层控制动作
- 策略暂停动作
- 风险事件数据库落库

## 通知系统

- 邮件配置装配
- 真实邮件发送接入风控生产实例
- 通知发送状态数据库落库
- 通知失败重试
- 通知已读/未读更新
- 全部通知中心的类型映射
