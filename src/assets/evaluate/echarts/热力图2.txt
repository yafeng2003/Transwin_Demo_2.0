const models = ['博雅智巡', 'DeepSeek-V3', '移动大模型', 'QWen3'];
const dimensions = ['事实准确性', '逻辑合理性', '结构完整性', '语言规范性'];

const data = [
  [0, 0, 95], [1, 0, 58], [2, 0, 55], [3, 0, 42],
  [0, 1, 93], [1, 1, 74], [2, 1, 40], [3, 1, 61],
  [0, 2, 96], [1, 2, 63], [2, 2, 39], [3, 2, 60],
  [0, 3, 94], [1, 3, 76], [2, 3, 52], [3, 3, 78]
];

const formattedData = data.map(item => [item[0], item[1], item[2]]);

option = {
  tooltip: {
    position: 'top',
    formatter: function (params) {
      return `${dimensions[params.data[1]]}<br/>${models[params.data[0]]}: ${params.data[2]}`;
    }
  },
  grid: {
    height: '50%',
    top: '15%'
  },
  xAxis: {
    type: 'category',
    data: models,
    splitArea: { show: true },
    axisLabel: {
      interval: 0,
      color: '#',
      fontWeight: 'bold',
      fontSize: 18
    }
  },
  yAxis: {
    type: 'category',
    data: dimensions,
    splitArea: { show: true },
    axisLabel: {
      color: '#000',
      fontWeight: 'bold',
      fontSize: 18
    }
  },
  visualMap: {
    min: 0,
    max: 100,
    calculable: true,
    orient: 'horizontal',
    left: 'center',
    bottom: '20%',
    inRange: {
      color: ['#66BB66', '#FFCC66', '#CC6666']  // 柔和：绿→橙→红棕
    }
  },
  series: [
    {
      name: '模型评分',
      type: 'heatmap',
      data: formattedData,
      label: {
        show: true,
        color: '#000',
        fontWeight: 'bold'
      },
      emphasis: {
        itemStyle: {
          shadowBlur: 10,
          shadowColor: 'rgba(0, 0, 0, 0.3)'
        }
      }
    }
  ]
};
